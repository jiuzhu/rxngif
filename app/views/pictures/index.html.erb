<h1 style="color: <%= params[:color] %>">List of Pictures</h1>

<ul>
  <% @list_of_pictures.each do |pic| %>
    <li>
      <img width="200" src="<%= pic.source %>">
      <p><%= pic.caption %></p>
    </li>
  <% end %>
</ul>
